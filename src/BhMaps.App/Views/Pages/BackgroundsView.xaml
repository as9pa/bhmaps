<UserControl x:Class="BhMaps.App.Views.Pages.BackgroundsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:behaviors="clr-namespace:BhMaps.App.Behaviors"
             xmlns:controls="clr-namespace:BhMaps.App.Views.Controls"
             behaviors:SizeShortcuts.Target="{Binding}"
             PreviewKeyDown="OnPreviewKeyDown">
  <UserControl.Resources>
    <BooleanToVisibilityConverter x:Key="BoolToVis" />
  </UserControl.Resources>

  <DockPanel Margin="24,16,24,24">
    <!-- Addendum B: title, search, size and Add Image all on the header's one line. -->
    <controls:PageHeader DockPanel.Dock="Top" Title="Backgrounds">
      <controls:PageHeader.Actions>
        <StackPanel Orientation="Horizontal">
          <Grid Width="200" Height="{StaticResource ActionHeight}" VerticalAlignment="Center">
            <!-- Its own padding, not the style's: a TextBox reserves the whole of Geist's line box, which at this
                 size is taller than an action, so the box keeps 6 at the top and 0 at the bottom. 6 is Task 4's
                 measurement, not a typo; do not put 3 back. Tagged, not named: an element inside PageHeader's
                 content cannot take an x:Name, and RowKeys finds the box by this marker. -->
            <TextBox Padding="33,6,10,0"
                     AutomationProperties.Name="{Binding SearchPlaceholder}"
                     Style="{StaticResource FieldTextBox}"
                     Tag="SearchBox"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />

            <controls:Icon Margin="11,0,0,0"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Center"
                           Foreground="{StaticResource Text3Brush}"
                           Geometry="{StaticResource Icon.Search}"
                           Size="15" />

            <TextBlock Margin="34,0,11,0"
                       VerticalAlignment="Center"
                       IsHitTestVisible="False"
                       Text="{Binding SearchPlaceholder}">
              <TextBlock.Style>
                <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                  <Setter Property="Foreground" Value="{StaticResource Text3Brush}" />
                  <Setter Property="Visibility" Value="Collapsed" />
                  <Style.Triggers>
                    <DataTrigger Binding="{Binding SearchText}" Value="">
                      <Setter Property="Visibility" Value="Visible" />
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </TextBlock.Style>
            </TextBlock>
          </Grid>

          <controls:SizePicker Margin="16,0,0,0" VerticalAlignment="Center" Value="{Binding TileSize}" />

          <!-- Solid: adding a picture is this page's one primary action (spec 4). -->
          <Button Margin="12,0,0,0"
                  Command="{Binding AddPicturesCommand}"
                  Content="Add image"
                  controls:Icon.Glyph="{StaticResource Icon.Plus}"
                  IsEnabled="{Binding DataContext.CanWrite, RelativeSource={RelativeSource AncestorType=Window}}"
                  Style="{StaticResource PrimaryButton}" />
        </StackPanel>
      </controls:PageHeader.Actions>
    </controls:PageHeader>

    <!-- Addendum B, q4: the chip row ends at the last chip, and there is no bar at the foot of the page. Spec 4
         puts the page's one switch at the right end of the same line, which is why the row is a Grid rather than
         the ListBox alone. -->
    <Grid DockPanel.Dock="Top" Margin="0,4,0,8">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>

      <ListBox HorizontalAlignment="Left"
               ItemContainerStyle="{StaticResource ChipRow}"
               ItemsSource="{Binding Chips}"
               SelectedItem="{Binding SelectedChip}"
               Style="{StaticResource TileListBox}">
        <ListBox.ItemsPanel>
          <ItemsPanelTemplate>
            <StackPanel Orientation="Horizontal" />
          </ItemsPanelTemplate>
        </ListBox.ItemsPanel>
      </ListBox>

      <!-- 2.8: the rows are missing a pack on purpose, so the header says how many and goes to the page where
           that was done. -->
      <Button Grid.Column="1"
              Margin="0,0,12,0"
              HorizontalAlignment="Right"
              VerticalAlignment="Center"
              AutomationProperties.Name="{Binding HiddenPacksText}"
              Command="{Binding GoToPacksCommand}"
              Content="{Binding HiddenPacksText}"
              FocusVisualStyle="{StaticResource DialogFocusRing}"
              Foreground="{StaticResource Text2Brush}"
              Style="{StaticResource PlainButton}"
              ToolTip="Hidden on the Packs page. Click to go there."
              Visibility="{Binding ShowHiddenPacks, Converter={StaticResource BoolToVis}}" />

      <!-- Spec 4: one control for the one question every row was asking. No border and no box: a plus and a label,
           muted while it is off. A Button, so Space and Enter toggle it as a click does. -->
      <Button Grid.Column="2"
              HorizontalAlignment="Right"
              VerticalAlignment="Center"
              AutomationProperties.Name="{Binding PicturesLabel}"
              behaviors:Fold.IsOpen="{Binding ShowPictures}"
              Command="{Binding TogglePicturesCommand}"
              FocusVisualStyle="{StaticResource DialogFocusRing}"
              ToolTip="Shows your own pictures on every row"
              Visibility="{Binding HasPictures, Converter={StaticResource BoolToVis}}">
        <Button.Style>
          <Style TargetType="Button" BasedOn="{StaticResource PlainButton}">
            <Style.Triggers>
              <DataTrigger Binding="{Binding ShowPictures}" Value="True">
                <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
              </DataTrigger>
            </Style.Triggers>
          </Style>
        </Button.Style>
        <StackPanel Orientation="Horizontal">
          <ContentControl Style="{StaticResource PlusMinusGlyph}" />

          <!-- The colour is read back off the button: the implicit TextBlock style would otherwise beat the
               Foreground this one inherits, the same reason PlainButton templates its string content. -->
          <TextBlock Margin="8,0,0,0"
                     VerticalAlignment="Center"
                     Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                     Text="{Binding PicturesLabel}" />
        </StackPanel>
      </Button>
    </Grid>

    <Grid>
      <Grid>
        <Grid.Style>
          <Style TargetType="Grid">
            <Style.Triggers>
              <DataTrigger Binding="{Binding DataContext.GameFolderMissing, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                <Setter Property="Visibility" Value="Collapsed" />
              </DataTrigger>
            </Style.Triggers>
          </Style>
        </Grid.Style>
        <!-- The rows virtualise (addendum B). Recycling is safe here because nothing in a row binds a container
             property back into the view model: a row has no IsSelected to be written by a container that has just
             been handed to a different row. -->
        <ItemsControl ItemTemplate="{StaticResource MapRow}"
                      ItemsSource="{Binding Rows}"
                      KeyboardNavigation.DirectionalNavigation="Continue"
                      KeyboardNavigation.TabNavigation="Continue"
                      ScrollViewer.CanContentScroll="True"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.ScrollUnit="Pixel"
                      VirtualizingPanel.VirtualizationMode="Recycling">
          <ItemsControl.Template>
            <!-- An ItemsControl has no ScrollViewer of its own, and without one the VirtualizingStackPanel is
                 handed infinite height and realises every row. -->
            <ControlTemplate TargetType="ItemsControl">
              <ScrollViewer CanContentScroll="True"
                            Focusable="False"
                            Padding="{TemplateBinding Padding}"
                            behaviors:SmoothScroll.IsEnabled="True">
                <ItemsPresenter />
              </ScrollViewer>
            </ControlTemplate>
          </ItemsControl.Template>
          <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
              <VirtualizingStackPanel />
            </ItemsPanelTemplate>
          </ItemsControl.ItemsPanel>
        </ItemsControl>

        <!-- Addendum B: an empty list says why in one line, and offers the way back when a search caused it. -->
        <controls:EmptyState ActionCommand="{Binding ClearSearchCommand}"
                             ActionText="{Binding EmptyActionText}"
                             Text="{Binding EmptyText}">
          <controls:EmptyState.Style>
            <Style TargetType="controls:EmptyState">
              <Setter Property="Visibility" Value="Collapsed" />
              <Style.Triggers>
                <DataTrigger Binding="{Binding Rows.Count}" Value="0">
                  <Setter Property="Visibility" Value="Visible" />
                </DataTrigger>
              </Style.Triggers>
            </Style>
          </controls:EmptyState.Style>
        </controls:EmptyState>
      </Grid>
      <!-- 3.0: with no game folder there is nothing on this page to read or write, so the page itself says so
           and offers the one thing that fixes it. The picker, the save and the rescan behind it are the Settings
           page's own command; there is one of them. -->
      <controls:EmptyState ActionCommand="{Binding DataContext.SettingsPage.ChangeGameCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                           ActionText="Choose folder"
                           Text="Game folder not found. Choose the Brawlhalla mapArt folder to continue."
                           Visibility="{Binding DataContext.GameFolderMissing, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolToVis}}" />
    </Grid>
  </DockPanel>
</UserControl>
