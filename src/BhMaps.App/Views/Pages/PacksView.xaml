<UserControl x:Class="BhMaps.App.Views.Pages.PacksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:BhMaps.App.Views.Controls">
  <UserControl.Resources>
    <!-- The theme's buttons keep WPF's own focus visual, which is a dark dotted rectangle and invisible on this
         ground; measured in the app, a focused Export button showed nothing at all. Spec 7.8 wants keyboard focus
         visible on every action, so the page's buttons wear the same 60% ring the rows and tiles draw. -->
    <Style x:Key="ActionFocusVisual">
      <Setter Property="Control.Template">
        <Setter.Value>
          <ControlTemplate>
            <Border BorderBrush="{StaticResource TextBrush}"
                    BorderThickness="1"
                    CornerRadius="{StaticResource Radius}"
                    Opacity="0.6"
                    SnapsToDevicePixels="True" />
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="ActionPrimaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
      <Setter Property="FocusVisualStyle" Value="{StaticResource ActionFocusVisual}" />
    </Style>

    <Style x:Key="ActionOutlineButton" TargetType="Button" BasedOn="{StaticResource OutlineButton}">
      <Setter Property="FocusVisualStyle" Value="{StaticResource ActionFocusVisual}" />
    </Style>

    <Style x:Key="ActionPlainButton" TargetType="Button" BasedOn="{StaticResource PlainButton}">
      <Setter Property="FocusVisualStyle" Value="{StaticResource ActionFocusVisual}" />
    </Style>

    <!-- The clickable body of a row: everything left of the actions, so a click on the name or the counts opens
         the pack and a click on an action does only that action. Page-only, like the sidebar's own row styles.
         Keyboard focus draws the same 60% ring the tiles wear, because focus has to be visible here too
         (spec 7.8). -->
    <Style x:Key="PackRowBody" TargetType="Button">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="Padding" Value="14,12" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="FocusVisualStyle" Value="{x:Null}" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Grid>
              <Border x:Name="Chrome"
                      Padding="{TemplateBinding Padding}"
                      Background="{TemplateBinding Background}"
                      CornerRadius="{StaticResource Radius}">
                <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" />
              </Border>
              <Border x:Name="Ring"
                      BorderBrush="{StaticResource TextBrush}"
                      BorderThickness="1"
                      CornerRadius="{StaticResource Radius}"
                      IsHitTestVisible="False"
                      Opacity="0" />
            </Grid>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Chrome" Property="Background" Value="{StaticResource Surface2Brush}" />
              </Trigger>
              <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter TargetName="Ring" Property="Opacity" Value="0.6" />
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Chrome" Property="Opacity" Value="0.4" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </UserControl.Resources>

  <DockPanel Margin="24,16,24,24">
    <controls:PageHeader DockPanel.Dock="Top" Title="Packs">
      <controls:PageHeader.Actions>
        <!-- Disabled while the shell is busy, the same hop past the page to the window's MainViewModel the header
             itself makes (decision D11), so a second operation cannot be started from a live-looking button. -->
        <StackPanel IsEnabled="{Binding DataContext.IsNotBusy, RelativeSource={RelativeSource AncestorType=Window}}" Orientation="Horizontal">
          <Button Command="{Binding ImportCommand}" Content="Import folder" Style="{StaticResource ActionOutlineButton}" />
          <Button Margin="8,0,0,0"
                  Command="{Binding CaptureDefaultsCommand}"
                  Content="Capture defaults"
                  Style="{StaticResource ActionOutlineButton}" />
          <Button Margin="8,0,0,0"
                  Command="{Binding OpenLibraryCommand}"
                  Content="Open library"
                  Style="{StaticResource ActionPlainButton}" />
        </StackPanel>
      </controls:PageHeader.Actions>
    </controls:PageHeader>

    <Grid IsEnabled="{Binding DataContext.IsNotBusy, RelativeSource={RelativeSource AncestorType=Window}}">
      <ScrollViewer Focusable="False" VerticalScrollBarVisibility="Auto">
        <ItemsControl ItemsSource="{Binding Rows}">
          <ItemsControl.ItemContainerStyle>
            <!-- Without this a screen reader announces each row as its view model's type name, and the four
                 identically labelled action buttons inside it have nothing to say which pack they belong to. -->
            <Style TargetType="ContentPresenter">
              <Setter Property="AutomationProperties.Name" Value="{Binding Name}" />
            </Style>
          </ItemsControl.ItemContainerStyle>
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <Border Margin="0,0,0,8" Style="{StaticResource CardBorder}">
                <Grid>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                  </Grid.ColumnDefinitions>

                  <!-- The row's commands live on the page, so each button reaches past its own row to the list's
                       DataContext and hands its row back as the parameter. -->
                  <Button Grid.Column="0"
                          AutomationProperties.Name="{Binding Name, StringFormat=Open pack {0}}"
                          Command="{Binding DataContext.OpenCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                          CommandParameter="{Binding}"
                          Style="{StaticResource PackRowBody}">
                    <StackPanel>
                      <TextBlock FontWeight="Medium" Text="{Binding Name}" TextTrimming="CharacterEllipsis" />
                      <TextBlock Margin="0,3,0,0" Foreground="{StaticResource Text2Brush}" Text="{Binding CountsText}" />
                    </StackPanel>
                  </Button>

                  <StackPanel Grid.Column="1"
                              Margin="8,0,10,0"
                              VerticalAlignment="Center"
                              Orientation="Horizontal">
                    <Button Command="{Binding DataContext.ApplyAllCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                            CommandParameter="{Binding}"
                            Content="Apply all"
                            Style="{StaticResource ActionOutlineButton}" />
                    <Button Margin="6,0,0,0"
                            Command="{Binding DataContext.RemoveCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                            CommandParameter="{Binding}"
                            Content="Remove"
                            Style="{StaticResource ActionPlainButton}" />
                    <Button Margin="6,0,0,0"
                            Command="{Binding DataContext.ExportCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                            CommandParameter="{Binding}"
                            Content="Export"
                            Style="{StaticResource ActionPlainButton}" />
                    <Button Margin="6,0,0,0"
                            Command="{Binding DataContext.OpenFolderCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                            CommandParameter="{Binding}"
                            Content="Open folder"
                            Style="{StaticResource ActionPlainButton}" />
                  </StackPanel>
                </Grid>
              </Border>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
      </ScrollViewer>

      <!-- Empty library (spec 7.8): the same two actions the header offers, put where there is nothing else to
           look at. The one solid button on this page, and it is only ever on screen while the list is empty. -->
      <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <StackPanel.Style>
          <Style TargetType="StackPanel">
            <Setter Property="Visibility" Value="Collapsed" />
            <Style.Triggers>
              <DataTrigger Binding="{Binding IsEmpty}" Value="True">
                <Setter Property="Visibility" Value="Visible" />
              </DataTrigger>
            </Style.Triggers>
          </Style>
        </StackPanel.Style>
        <TextBlock HorizontalAlignment="Center" Foreground="{StaticResource Text2Brush}" Text="No packs yet." />
        <StackPanel Margin="0,14,0,0" HorizontalAlignment="Center" Orientation="Horizontal">
          <Button Command="{Binding CaptureDefaultsCommand}" Content="Capture defaults" Style="{StaticResource ActionPrimaryButton}" />
          <Button Margin="8,0,0,0"
                  Command="{Binding ImportCommand}"
                  Content="Import folder"
                  Style="{StaticResource ActionOutlineButton}" />
        </StackPanel>
      </StackPanel>
    </Grid>
  </DockPanel>
</UserControl>
