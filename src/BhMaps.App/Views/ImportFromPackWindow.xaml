<Window x:Class="BhMaps.App.Views.ImportFromPackWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="460" Height="560" ResizeMode="NoResize"
        WindowStyle="None"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="{StaticResource BgBrush}"
        AutomationProperties.Name="{Binding Title}"
        Title="{Binding Title}"
        KeyDown="OnKeyDown">
  <Window.Resources>
    <BooleanToVisibilityConverter x:Key="BoolToVis" />

    <!-- The row's right-hand word. "already here" is the one that has to be read as a warning, and a Style
         trigger is the only way to say that from inside one shared template. -->
    <Style x:Key="RowDetailText" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
      <Setter Property="VerticalAlignment" Value="Center" />
      <Setter Property="FontSize" Value="11" />
      <Setter Property="Foreground" Value="{StaticResource Text3Brush}" />
      <Style.Triggers>
        <DataTrigger Binding="{Binding AlreadyHere}" Value="True">
          <Setter Property="Foreground" Value="{StaticResource MissingFgBrush}" />
        </DataTrigger>
      </Style.Triggers>
    </Style>

    <!-- The checkbox inside the row is what ticks it, so the container is never a tab stop of its own. -->
    <Style x:Key="ImportRowItem" TargetType="ListBoxItem" BasedOn="{StaticResource TileListBoxItem}">
      <Setter Property="Focusable" Value="False" />
      <Setter Property="Padding" Value="6,5" />
      <Setter Property="AutomationProperties.Name" Value="{Binding Name}" />
    </Style>

    <DataTemplate x:Key="ImportRowTemplate">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Border Width="40" Height="22" CornerRadius="3"
                RenderOptions.BitmapScalingMode="HighQuality">
          <Border.Background>
            <ImageBrush ImageSource="{Binding Thumbnail}" Stretch="UniformToFill" />
          </Border.Background>
        </Border>
        <CheckBox Grid.Column="1" Margin="10,0,10,0" VerticalAlignment="Center"
                  AutomationProperties.Name="{Binding Name}"
                  Content="{Binding Name}"
                  FocusVisualStyle="{StaticResource DialogFocusRing}"
                  IsChecked="{Binding IsTicked, Mode=TwoWay}"
                  IsEnabled="{Binding IsEnabled}" />
        <TextBlock Grid.Column="2" Style="{StaticResource RowDetailText}" Text="{Binding Detail}" />
      </Grid>
    </DataTemplate>
  </Window.Resources>
  <Border BorderBrush="{StaticResource LineBrush}"
          BorderThickness="1"
          CornerRadius="{StaticResource Radius}">
    <Grid Margin="20">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>

      <TextBlock FontSize="17" FontWeight="SemiBold" Text="{Binding Title}" TextWrapping="Wrap" />
      <TextBlock Grid.Row="1" Margin="0,6,0,0" FontSize="12" Foreground="{StaticResource Text3Brush}"
                 Text="{Binding Subtitle}" TextWrapping="Wrap" />

      <StackPanel Grid.Row="2" Margin="0,14,0,0">
        <TextBlock Foreground="{StaticResource Text2Brush}" Text="From" />
        <ComboBox x:Name="From" Margin="0,6,0,0" AutomationProperties.Name="From"
                  DisplayMemberPath="Label"
                  FocusVisualStyle="{StaticResource DialogFocusRing}"
                  ItemsSource="{Binding Sources}"
                  SelectedItem="{Binding Source, Mode=TwoWay}" />
        <TextBlock Margin="0,14,0,0" Foreground="{StaticResource Text2Brush}" Text="Which maps" />
        <RadioButton Margin="0,6,0,0" Content="{Binding AllMapsText}"
                     FocusVisualStyle="{StaticResource DialogFocusRing}"
                     GroupName="Which" IsChecked="{Binding AllMaps, Mode=TwoWay}" />
        <RadioButton Margin="0,4,0,0" Content="Choose maps"
                     FocusVisualStyle="{StaticResource DialogFocusRing}" GroupName="Which" />
      </StackPanel>

      <!-- The two lists are up only while the second radio is: with All chosen there is nothing to tick. -->
      <Grid Grid.Row="3" Margin="0,12,0,0"
            Visibility="{Binding ChooseMaps, Converter={StaticResource BoolToVis}}">
        <Grid.RowDefinitions>
          <RowDefinition Height="*" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ListBox AutomationProperties.Name="Maps to import"
                 Focusable="False"
                 FocusVisualStyle="{StaticResource DialogFocusRing}"
                 ItemContainerStyle="{StaticResource ImportRowItem}"
                 ItemsSource="{Binding Rows}"
                 ItemTemplate="{StaticResource ImportRowTemplate}"
                 KeyboardNavigation.TabNavigation="Continue"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled" />

        <TextBlock Grid.Row="1" Margin="0,12,0,6"
                   Foreground="{StaticResource Text2Brush}" Text="Pictures"
                   Visibility="{Binding HasLooseRows, Converter={StaticResource BoolToVis}}" />
        <ListBox Grid.Row="2" MaxHeight="120"
                 AutomationProperties.Name="Pictures to import"
                 Focusable="False"
                 FocusVisualStyle="{StaticResource DialogFocusRing}"
                 ItemContainerStyle="{StaticResource ImportRowItem}"
                 ItemsSource="{Binding LooseRows}"
                 ItemTemplate="{StaticResource ImportRowTemplate}"
                 KeyboardNavigation.TabNavigation="Continue"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 Visibility="{Binding HasLooseRows, Converter={StaticResource BoolToVis}}" />
      </Grid>

      <CheckBox Grid.Row="4" Margin="0,12,0,0"
                Content="{Binding ReplaceText}"
                FocusVisualStyle="{StaticResource DialogFocusRing}"
                IsChecked="{Binding Replace, Mode=TwoWay}" />

      <StackPanel Grid.Row="5" Margin="0,16,0,0" HorizontalAlignment="Right" Orientation="Horizontal">
        <Button Margin="0,0,8,0" AutomationProperties.Name="Cancel" Content="Cancel" IsCancel="True"
                FocusVisualStyle="{StaticResource DialogFocusRing}" Style="{StaticResource OutlineButton}" />
        <Button AutomationProperties.Name="{Binding PrimaryText}" Click="OnImport"
                Content="{Binding PrimaryText}" IsDefault="True" IsEnabled="{Binding CanImport}"
                FocusVisualStyle="{StaticResource DialogFocusRing}" Style="{StaticResource PrimaryButton}" />
      </StackPanel>
    </Grid>
  </Border>
</Window>
