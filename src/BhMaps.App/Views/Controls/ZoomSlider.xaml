<UserControl x:Class="BhMaps.App.Views.Controls.ZoomSlider"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:BhMaps.App.Views.Controls"
             x:Name="Root">
  <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
    <!-- The glyphs say which end is which: a few big tiles at the Minimum end, many small ones at the Maximum
         end. Muted, because they label the slider rather than ask to be read. -->
    <controls:Icon Margin="0,0,8,0"
                   VerticalAlignment="Center"
                   Foreground="{StaticResource Text3Brush}"
                   Geometry="{StaticResource Icon.GridLarge}"
                   Size="16" />
    <Slider AutomationProperties.Name="{Binding Label, ElementName=Root}"
            Maximum="{Binding Maximum, ElementName=Root}"
            Minimum="{Binding Minimum, ElementName=Root}"
            Style="{StaticResource ZoomSliderStyle}"
            Value="{Binding Value, ElementName=Root, Mode=TwoWay}" />
    <controls:Icon Margin="8,0,0,0"
                   VerticalAlignment="Center"
                   Foreground="{StaticResource Text3Brush}"
                   Geometry="{StaticResource Icon.GridSmall}"
                   Size="16" />
  </StackPanel>
</UserControl>
