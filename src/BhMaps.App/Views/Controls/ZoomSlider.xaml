<UserControl x:Class="BhMaps.App.Views.Controls.ZoomSlider"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root">
  <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
    <!-- The glyphs say which end is which: a big card at the Minimum end, a small one at the Maximum end. -->
    <Rectangle Width="10"
               Height="10"
               Margin="0,0,8,0"
               VerticalAlignment="Center"
               Fill="{StaticResource Text3Brush}"
               RadiusX="2"
               RadiusY="2" />
    <Slider AutomationProperties.Name="Columns"
            Maximum="{Binding Maximum, ElementName=Root}"
            Minimum="{Binding Minimum, ElementName=Root}"
            Style="{StaticResource ZoomSliderStyle}"
            Value="{Binding Value, ElementName=Root, Mode=TwoWay}" />
    <Rectangle Width="6"
               Height="6"
               Margin="8,0,0,0"
               VerticalAlignment="Center"
               Fill="{StaticResource Text3Brush}"
               RadiusX="1"
               RadiusY="1" />
    <TextBlock Margin="8,0,0,0"
               VerticalAlignment="Center"
               Style="{StaticResource MonoText}"
               Text="{Binding Value, ElementName=Root}" />
  </StackPanel>
</UserControl>
