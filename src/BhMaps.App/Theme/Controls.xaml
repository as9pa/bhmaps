<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:behaviors="clr-namespace:BhMaps.App.Behaviors"
                    xmlns:controls="clr-namespace:BhMaps.App.Views.Controls"
                    xmlns:converters="clr-namespace:BhMaps.App.Converters"
                    xmlns:sys="clr-namespace:System;assembly=System.Runtime"
                    xmlns:vm="clr-namespace:BhMaps.App.ViewModels">
  <ResourceDictionary.MergedDictionaries>
    <!-- Fonts as well as Tokens, so this dictionary resolves its own StaticResource references
         whether it is loaded on its own or merged after them by App.xaml. -->
    <ResourceDictionary Source="Fonts.xaml" />
    <ResourceDictionary Source="Tokens.xaml" />
  </ResourceDictionary.MergedDictionaries>

  <BooleanToVisibilityConverter x:Key="BoolToVis" />

  <!-- Implicit, so plain text anywhere in the app is Geist at the body size. Keyed TextBlock
       styles below derive from it with BasedOn; an explicit Style would otherwise replace it. -->
  <Style TargetType="TextBlock">
    <Setter Property="FontFamily" Value="{StaticResource Sans}" />
    <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
    <Setter Property="FontSize" Value="{StaticResource FontBody}" />
  </Style>

  <!-- The button styles below replace the theme's own, so they lose its focus visual with it, and the stock
       dashed adorner is close to invisible on this palette. They wear this instead, and so do the chips, the
       tick boxes, the section headers and a dialog's checkboxes and radios: one 1 px accent ring at 60% standing
       2 px off the control, in the adorner layer, so it costs the shared templates nothing (spec 7.8). Only keyboard
       focus raises it, which is what a FocusVisualStyle already does; the mouse never draws one. Declared
       first because a StaticResource cannot reach forward inside one dictionary. -->
  <Style x:Key="DialogFocusRing">
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate>
          <!-- Radius plus the 2 px it stands off by, written out because XAML cannot add two tokens. -->
          <Border Margin="-2"
                  BorderBrush="{StaticResource FocusRingBrush}"
                  BorderThickness="1"
                  CornerRadius="8"
                  Opacity="0.6"
                  SnapsToDevicePixels="True" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- The only filled button. One per page, for that page's single primary action. -->
  <Style x:Key="PrimaryButton" TargetType="Button">
    <Setter Property="FontFamily" Value="{StaticResource Sans}" />
    <Setter Property="FontSize" Value="{StaticResource FontBody}" />
    <Setter Property="Background" Value="{StaticResource AccentBrush}" />
    <Setter Property="Foreground" Value="{StaticResource BgBrush}" />
    <!-- The pressed fill travels on BorderBrush, which a borderless template has no other use for: a style
         BasedOn this one, such as DangerButton, then gives the press its own colour without a second template. -->
    <Setter Property="BorderBrush" Value="{StaticResource AccentPressedBrush}" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Padding" Value="14,7" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="FocusVisualStyle" Value="{StaticResource DialogFocusRing}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <ControlTemplate.Resources>
            <!-- The TextBlock ContentPresenter makes for string content has no logical parent, so the
                 implicit style still reaches it from Application.Resources and beats this button's own
                 Foreground. Templating the string writes those values back as local values, which win. -->
            <!-- Underscores render literally: no keyboard mnemonics in this app, and labels carry pack names. -->
            <DataTemplate DataType="{x:Type sys:String}">
              <TextBlock Text="{Binding}"
                         FontFamily="{Binding FontFamily, RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}}}"
                         FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}}}"
                         Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}}}" />
            </DataTemplate>
          </ControlTemplate.Resources>
          <!-- The canvas draws a 15 px line icon 7 px before a button's label. It is an attached property rather
               than part of the content, so the label stays a string: the accessible name a screen reader reads is
               still the Content, and the icon takes the Foreground the button hands down, hover and disabled with
               it. A button given no glyph collapses the slot and measures exactly as it did before. -->
          <Border x:Name="Chrome"
                  Background="{TemplateBinding Background}"
                  CornerRadius="{StaticResource Radius}"
                  Padding="{TemplateBinding Padding}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Horizontal">
              <controls:Icon x:Name="Glyph"
                             Margin="0,0,7,0"
                             VerticalAlignment="Center"
                             Geometry="{Binding Path=(controls:Icon.Glyph), RelativeSource={RelativeSource TemplatedParent}}"
                             Size="15" />
              <ContentPresenter VerticalAlignment="Center" />
            </StackPanel>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="controls:Icon.Glyph" Value="{x:Null}">
              <Setter TargetName="Glyph" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Chrome" Property="Opacity" Value="0.9" />
            </Trigger>
            <!-- After hover, so pressing replaces the lift rather than compounding with it. -->
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="Chrome" Property="Background" Value="{Binding BorderBrush, RelativeSource={RelativeSource TemplatedParent}}" />
              <Setter TargetName="Chrome" Property="Opacity" Value="1" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="Chrome" Property="Opacity" Value="0.4" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- The primary of a confirm whose verb deletes or removes something: PrimaryButton's shape, padding and
       states in Missing's colour, which is already the colour the app gives art that is gone. -->
  <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
    <Setter Property="Background" Value="{StaticResource MissingFgBrush}" />
    <Setter Property="BorderBrush" Value="{StaticResource MissingPressedBrush}" />
  </Style>

  <Style x:Key="OutlineButton" TargetType="Button">
    <Setter Property="FontFamily" Value="{StaticResource Sans}" />
    <Setter Property="FontSize" Value="{StaticResource FontBody}" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderBrush" Value="{StaticResource Line2Brush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
    <Setter Property="Padding" Value="14,7" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="FocusVisualStyle" Value="{StaticResource DialogFocusRing}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <ControlTemplate.Resources>
            <!-- The TextBlock ContentPresenter makes for string content has no logical parent, so the
                 implicit style still reaches it from Application.Resources and beats this button's own
                 Foreground. Templating the string writes those values back as local values, which win. -->
            <DataTemplate DataType="{x:Type sys:String}">
              <TextBlock Text="{Binding}"
                         FontFamily="{Binding FontFamily, RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}}}"
                         FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}}}"
                         Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}}}" />
            </DataTemplate>
          </ControlTemplate.Resources>
          <!-- The icon slot PrimaryButton explains. -->
          <Border x:Name="Chrome"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="{StaticResource Radius}"
                  Padding="{TemplateBinding Padding}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Horizontal">
              <controls:Icon x:Name="Glyph"
                             Margin="0,0,7,0"
                             VerticalAlignment="Center"
                             Geometry="{Binding Path=(controls:Icon.Glyph), RelativeSource={RelativeSource TemplatedParent}}"
                             Size="15" />
              <ContentPresenter VerticalAlignment="Center" />
            </StackPanel>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="controls:Icon.Glyph" Value="{x:Null}">
              <Setter TargetName="Glyph" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Chrome" Property="Background" Value="{StaticResource Surface2Brush}" />
            </Trigger>
            <!-- After hover, so pressing replaces it: a step back down to Surface, darker than the hover lift.
                 TileAction rides this template too, and it is only ever seen hovered, so hover to pressed is
                 the change that has to read there. -->
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="Chrome" Property="Background" Value="{StaticResource SurfaceBrush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="Chrome" Property="Opacity" Value="0.4" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="PlainButton" TargetType="Button">
    <Setter Property="FontFamily" Value="{StaticResource Sans}" />
    <Setter Property="FontSize" Value="{StaticResource FontBody}" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Foreground" Value="{StaticResource Text2Brush}" />
    <Setter Property="Padding" Value="14,7" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="FocusVisualStyle" Value="{StaticResource DialogFocusRing}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <ControlTemplate.Resources>
            <!-- The TextBlock ContentPresenter makes for string content has no logical parent, so the
                 implicit style still reaches it from Application.Resources and beats this button's own
                 Foreground. Templating the string writes those values back as local values, which win. -->
            <DataTemplate DataType="{x:Type sys:String}">
              <TextBlock Text="{Binding}"
                         FontFamily="{Binding FontFamily, RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}}}"
                         FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}}}"
                         Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}}}" />
            </DataTemplate>
          </ControlTemplate.Resources>
          <!-- The icon slot PrimaryButton explains. Hover here lifts the Foreground rather than the fill, and the
               icon follows it, because Stroke is bound to the Foreground this button hands down. -->
          <Border x:Name="Chrome"
                  Background="{TemplateBinding Background}"
                  CornerRadius="{StaticResource Radius}"
                  Padding="{TemplateBinding Padding}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Horizontal">
              <controls:Icon x:Name="Glyph"
                             Margin="0,0,7,0"
                             VerticalAlignment="Center"
                             Geometry="{Binding Path=(controls:Icon.Glyph), RelativeSource={RelativeSource TemplatedParent}}"
                             Size="15" />
              <ContentPresenter VerticalAlignment="Center" />
            </StackPanel>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="controls:Icon.Glyph" Value="{x:Null}">
              <Setter TargetName="Glyph" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            </Trigger>
            <!-- After hover, so pressing replaces it. Plain lifts nothing on hover, so the fill is the press. -->
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="Chrome" Property="Background" Value="{StaticResource Surface2Brush}" />
              <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="Chrome" Property="Opacity" Value="0.4" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- The links the status strip offers, and the first-run line under the top bar with them: a link here is the
       app's plain button with the text's own weight and rule on it, never a colour, because the strip's dot is
       the only coloured thing on either line. -->
  <Style x:Key="StripLink" TargetType="Button" BasedOn="{StaticResource PlainButton}">
    <Setter Property="Padding" Value="{StaticResource Gap8}" />
  </Style>

  <Style x:Key="StripLinkText" TargetType="TextBlock">
    <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
    <Setter Property="FontSize" Value="{StaticResource FontBody}" />
    <Setter Property="FontWeight" Value="SemiBold" />
    <Setter Property="TextDecorations" Value="Underline" />
  </Style>

  <Style x:Key="ChipToggle" TargetType="ToggleButton">
    <Setter Property="FontFamily" Value="{StaticResource Sans}" />
    <Setter Property="FontSize" Value="{StaticResource FontBody}" />
    <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
    <Setter Property="BorderBrush" Value="{StaticResource LineBrush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Foreground" Value="{StaticResource Text2Brush}" />
    <Setter Property="Padding" Value="12,5" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="FocusVisualStyle" Value="{StaticResource DialogFocusRing}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ToggleButton">
          <ControlTemplate.Resources>
            <!-- See PrimaryButton: the checked chip's Bg-coloured label needs the same handover. -->
            <DataTemplate DataType="{x:Type sys:String}">
              <TextBlock Text="{Binding}"
                         FontFamily="{Binding FontFamily, RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}}}"
                         FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}}}"
                         Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}}}" />
            </DataTemplate>
          </ControlTemplate.Resources>
          <Border x:Name="Chrome"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="{StaticResource RadiusPill}"
                  Padding="{TemplateBinding Padding}">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Chrome" Property="Background" Value="{StaticResource Surface2Brush}" />
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="Chrome" Property="Background" Value="{StaticResource TextBrush}" />
              <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource TextBrush}" />
              <Setter Property="Foreground" Value="{StaticResource BgBrush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="Chrome" Property="Opacity" Value="0.4" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- The tick box on the platform editor's file rows (ruling 10): an 18 px box, filled in the accent with a Bg
       tick when the row is ticked. It carries the shared ring like every other control, though today the box is
       Focusable="False" and the row behind it is the tab stop, so nothing raises it. -->
  <Style x:Key="CardTick" TargetType="CheckBox">
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="FocusVisualStyle" Value="{StaticResource DialogFocusRing}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="CheckBox">
          <Border x:Name="Box"
                  Width="18"
                  Height="18"
                  Background="{StaticResource BgBrush}"
                  BorderBrush="{StaticResource Line2Brush}"
                  BorderThickness="1"
                  CornerRadius="4"
                  Opacity="0.95">
            <Path x:Name="Tick"
                  Width="10"
                  Height="7"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"
                  Data="M0,3.4 L2.8,6.2 L8,0.6"
                  Stretch="Uniform"
                  Stroke="{StaticResource BgBrush}"
                  StrokeEndLineCap="Round"
                  StrokeLineJoin="Round"
                  StrokeStartLineCap="Round"
                  StrokeThickness="1.6"
                  Visibility="Collapsed" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="Box" Property="Background" Value="{StaticResource AccentBrush}" />
              <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
              <Setter TargetName="Tick" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource TextBrush}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <converters:IsCurrentPageConverter x:Key="IsCurrentPage" />

  <!-- One of the top bar's four destinations (spec 2.1). A Button, not a ToggleButton: the open page is a
       DataTrigger on the shell's CurrentPage, and a ToggleButton would clobber a one-way IsChecked binding on
       its first click. Tag carries the page this tab opens. -->
  <Style x:Key="TopBarTab" TargetType="Button">
    <Setter Property="FontFamily" Value="{StaticResource Sans}" />
    <Setter Property="FontSize" Value="{StaticResource FontBody}" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="Foreground" Value="{StaticResource Text2Brush}" />
    <Setter Property="Padding" Value="12,5" />
    <Setter Property="Margin" Value="0,0,4,0" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <ControlTemplate.Resources>
            <!-- See PrimaryButton: the implicit TextBlock style would beat the button's own Foreground. -->
            <DataTemplate DataType="{x:Type sys:String}">
              <TextBlock Text="{Binding}"
                         FontFamily="{Binding FontFamily, RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}}}"
                         FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}}}"
                         Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}}}" />
            </DataTemplate>
          </ControlTemplate.Resources>
          <Grid>
            <Border x:Name="Chrome"
                    Background="{TemplateBinding Background}"
                    CornerRadius="{StaticResource Radius}"
                    Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <Border x:Name="Ring"
                    BorderBrush="{StaticResource FocusRingBrush}"
                    BorderThickness="1"
                    CornerRadius="{StaticResource Radius}"
                    IsHitTestVisible="False"
                    Opacity="0" />
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <Setter TargetName="Ring" Property="Opacity" Value="0.6" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <!-- Hover first, the open page second, so the open tab keeps its fill while the pointer is over it. A
         trigger's binding cannot reach its TemplatedParent, which is why both live out here. -->
    <Style.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter Property="Background" Value="{StaticResource Surface2Brush}" />
        <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
      </Trigger>
      <DataTrigger Value="True">
        <DataTrigger.Binding>
          <MultiBinding Converter="{StaticResource IsCurrentPage}">
            <Binding Path="Tag" RelativeSource="{RelativeSource Self}" />
            <Binding Path="CurrentPage" />
          </MultiBinding>
        </DataTrigger.Binding>
        <Setter Property="Background" Value="{StaticResource TileBrush}" />
        <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
      </DataTrigger>
    </Style.Triggers>
  </Style>

  <Style x:Key="CardBorder" TargetType="Border">
    <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
    <Setter Property="BorderBrush" Value="{StaticResource LineBrush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="CornerRadius" Value="{StaticResource Radius}" />
  </Style>

  <!-- Style.Resources reaches the tag's own TextBlock child, so a tag is one Border plus a TextBlock. -->
  <Style x:Key="StateTagBorder" TargetType="Border">
    <Style.Resources>
      <Style TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource Sans}" />
        <Setter Property="FontSize" Value="{StaticResource FontCaption}" />
        <Setter Property="Foreground" Value="{StaticResource Text2Brush}" />
      </Style>
    </Style.Resources>
    <Setter Property="Background" Value="{StaticResource Surface2Brush}" />
    <Setter Property="CornerRadius" Value="4" />
    <Setter Property="Padding" Value="6,2" />
  </Style>

  <!-- Missing is the only coloured state (D8). -->
  <Style x:Key="MissingTagBorder" TargetType="Border">
    <Style.Resources>
      <Style TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource Sans}" />
        <Setter Property="FontSize" Value="{StaticResource FontCaption}" />
        <Setter Property="Foreground" Value="{StaticResource MissingFgBrush}" />
      </Style>
    </Style.Resources>
    <Setter Property="Background" Value="{StaticResource MissingBgBrush}" />
    <Setter Property="CornerRadius" Value="4" />
    <Setter Property="Padding" Value="6,2" />
  </Style>

  <Style x:Key="FieldTextBox" TargetType="TextBox">
    <Setter Property="FontFamily" Value="{StaticResource Sans}" />
    <Setter Property="FontSize" Value="{StaticResource FontBody}" />
    <Setter Property="Background" Value="{StaticResource Surface2Brush}" />
    <Setter Property="BorderBrush" Value="{StaticResource Line2Brush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
    <Setter Property="CaretBrush" Value="{StaticResource TextBrush}" />
    <Setter Property="SelectionBrush" Value="{StaticResource Text3Brush}" />
    <Setter Property="Padding" Value="10,6" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="TextBox">
          <Border x:Name="Chrome"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="{StaticResource Radius}">
            <!-- No Margin binding here on purpose: TextBoxBase puts the TextBox's Padding onto PART_ContentHost
                 itself when it attaches the render scope, so binding Padding to Margin as well applied the inset
                 twice and started typed text 33 px past this field's left padding (spec 9). The stock templates
                 leave this alone for the same reason. -->
            <ScrollViewer x:Name="PART_ContentHost"
                          Focusable="False"
                          HorizontalScrollBarVisibility="Hidden"
                          VerticalScrollBarVisibility="Hidden" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
              <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource TextBrush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="Chrome" Property="Opacity" Value="0.4" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- Selection is a ring, not a fill, so a tile's own artwork keeps its colours. Selection is 2 px in the
       accent; keyboard focus draws the same ring 1 px at 60%, so focus is visible even on an unselected tile
       (spec 7.8). The ring is an overlay in the Grid rather than a border around the content, so the 2 px the
       open card wears moves nothing. -->
  <Style x:Key="TileListBoxItem" TargetType="ListBoxItem">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="Padding" Value="0" />
    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
    <Setter Property="VerticalContentAlignment" Value="Stretch" />
    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ListBoxItem">
          <Grid Background="{TemplateBinding Background}">
            <ContentPresenter Margin="{TemplateBinding Padding}"
                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
            <Border x:Name="Ring"
                    BorderBrush="{StaticResource FocusRingBrush}"
                    BorderThickness="1"
                    CornerRadius="{StaticResource Radius}"
                    IsHitTestVisible="False"
                    Opacity="0" />
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <Setter TargetName="Ring" Property="Opacity" Value="0.6" />
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
              <Setter TargetName="Ring" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
              <Setter TargetName="Ring" Property="BorderThickness" Value="2" />
              <Setter TargetName="Ring" Property="Opacity" Value="1" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="TileListBox" TargetType="ListBox">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Padding" Value="0" />
    <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
    <Setter Property="ItemContainerStyle" Value="{StaticResource TileListBoxItem}" />
    <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
  </Style>

  <!-- The app's one slider: a 2 px Line2 groove with a 12 px Text thumb, no ticks drawn. Shared by the zoom
       sliders and by the editor's, so the two cannot drift. The disabled look is on each style rather than in
       here, because the editor dims a whole row at 45% and two opacities would multiply (decision C-D9). -->
  <ControlTemplate x:Key="SliderTrack" TargetType="Slider">
    <ControlTemplate.Resources>
      <!-- The two halves of the groove are invisible; the track line is drawn once, behind. -->
      <Style x:Key="GrooveHalf" TargetType="RepeatButton">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="RepeatButton">
              <Border Background="Transparent" />
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
    </ControlTemplate.Resources>
    <Grid Height="20" Background="Transparent">
      <Border Height="2"
              VerticalAlignment="Center"
              Background="{StaticResource Line2Brush}"
              CornerRadius="1" />
      <Track x:Name="PART_Track">
        <Track.DecreaseRepeatButton>
          <RepeatButton Command="Slider.DecreaseLarge" Style="{StaticResource GrooveHalf}" />
        </Track.DecreaseRepeatButton>
        <Track.IncreaseRepeatButton>
          <RepeatButton Command="Slider.IncreaseLarge" Style="{StaticResource GrooveHalf}" />
        </Track.IncreaseRepeatButton>
        <Track.Thumb>
          <Thumb Width="12" Height="12" Focusable="False">
            <Thumb.Template>
              <ControlTemplate TargetType="Thumb">
                <Ellipse Fill="{StaticResource TextBrush}" />
              </ControlTemplate>
            </Thumb.Template>
          </Thumb>
        </Track.Thumb>
      </Track>
    </Grid>
  </ControlTemplate>

  <!-- Spec 7.2: the editor's sliders. Move-to-point on, so a click on the groove jumps the thumb there instead
       of stepping, and SliderDrag so that holding the button after that goes on dragging it; no snapping,
       because pan is continuous. The row around a disabled one carries the dimming. -->
  <Style x:Key="EditorSlider" TargetType="Slider">
    <Setter Property="Orientation" Value="Horizontal" />
    <Setter Property="IsMoveToPointEnabled" Value="True" />
    <Setter Property="IsSnapToTickEnabled" Value="False" />
    <Setter Property="VerticalAlignment" Value="Center" />
    <Setter Property="Template" Value="{StaticResource SliderTrack}" />
    <Setter Property="behaviors:SliderDrag.IsEnabled" Value="True" />
  </Style>

  <!-- Spec 7.3: the one progress bar in the app, three pixels of it, under the download line. -->
  <Style x:Key="ThinProgress" TargetType="ProgressBar">
    <Setter Property="Height" Value="3" />
    <Setter Property="Background" Value="{StaticResource Line2Brush}" />
    <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Maximum" Value="1" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ProgressBar">
          <Grid>
            <Border Background="{TemplateBinding Background}" CornerRadius="{StaticResource Radius}" />
            <Border x:Name="PART_Track" />
            <Border x:Name="PART_Indicator"
                    HorizontalAlignment="Left"
                    Background="{TemplateBinding Foreground}"
                    CornerRadius="{StaticResource Radius}" />
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="SectionHeaderText" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
    <Setter Property="FontSize" Value="{StaticResource FontCaption}" />
    <Setter Property="FontWeight" Value="Medium" />
    <Setter Property="Foreground" Value="{StaticResource Text3Brush}" />
  </Style>

  <Style x:Key="MonoText" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
    <Setter Property="FontFamily" Value="{StaticResource Mono}" />
    <Setter Property="FontSize" Value="{StaticResource FontCaption}" />
    <Setter Property="Foreground" Value="{StaticResource Text2Brush}" />
  </Style>

  <!-- Spec section 13: the menus are themed rather than left as the platform's. BasedOn the Fluent implicit style,
       the lesson Views/AddPicturesWindow.xaml line 40 records: without it these fall off the Fluent theme
       (ThemeMode=Dark in App.xaml) onto the legacy template, which on this palette draws a light menu. Setters
       only, no re-template: the roles a MenuItem template carries (submenu, check column, icon column) are not
       worth re-implementing for a five line menu. -->
  <Style x:Key="TileContextMenu" TargetType="ContextMenu" BasedOn="{StaticResource {x:Type ContextMenu}}">
    <Setter Property="FontFamily" Value="{StaticResource Sans}" />
    <Setter Property="FontSize" Value="{StaticResource FontBody}" />
    <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
    <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
    <Setter Property="BorderBrush" Value="{StaticResource Line2Brush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Padding" Value="{StaticResource Gap4}" />
  </Style>

  <!-- For a menu whose rows are not TileMenuCommand: the header is the words themselves, drawn the same way and
       still not through AccessText. A style based on TileMenuItem that sets its own Header sets this beside it,
       because TileMenuItem's own template reads Text and Gesture off the line. -->
  <DataTemplate x:Key="TileMenuPlainHeader">
    <TextBlock Text="{Binding}" />
  </DataTemplate>

  <!-- The header's detail line only exists on a header that has one, and a header with none must not leave a
       gap above its hairline. -->
  <converters:NullToVisibilityConverter x:Key="NullToVis" />

  <!-- Header and Command are setters rather than per item markup, so one ItemsSource of TileMenuCommand drives
       every tile's menu and a page never writes a MenuItem by hand. A literal MenuItem that sets its own Header
       still wins: a local value beats a style setter. -->
  <Style x:Key="TileMenuItem" TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}">
    <Setter Property="FontFamily" Value="{StaticResource Sans}" />
    <Setter Property="FontSize" Value="{StaticResource FontBody}" />
    <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
    <Setter Property="Padding" Value="10,6" />
    <Setter Property="Header" Value="{Binding}" />

    <!-- A file or pack name reads exactly as typed. Without a template the header string is drawn through
         AccessText, which eats the underscore in "BG_Dojo.jpg" and underlines the D instead; no menu in spec
         section 11 offers an access key, so no menu needs one. The header is the line itself, so the template
         can draw its gesture beside its words. -->
    <Setter Property="HeaderTemplate">
      <Setter.Value>
        <DataTemplate>
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding Text}" />
            <TextBlock Grid.Column="1" Margin="24,0,0,0" Foreground="{StaticResource Text3Brush}"
                       Text="{Binding Gesture}" />
          </Grid>
        </DataTemplate>
      </Setter.Value>
    </Setter>
    <Setter Property="Command" Value="{Binding Command}" />
    <Setter Property="AutomationProperties.Name" Value="{Binding Text}" />

    <!-- Spec 4.3's flyouts: an item with Children is a submenu, and its own rows are containered by this same
         style. DynamicResource because a style cannot StaticResource itself. -->
    <Setter Property="ItemsSource" Value="{Binding Children}" />
    <Setter Property="ItemContainerStyle" Value="{DynamicResource TileMenuItem}" />

    <!-- Spec 4.3: a pack that holds nothing for the target is offered and refused rather than hidden, so the
         menu's shape does not change with the target. WPF hides a tooltip on a disabled item without the third. -->
    <Setter Property="IsEnabled" Value="{Binding IsEnabled}" />
    <Setter Property="ToolTip" Value="{Binding ToolTip}" />
    <Setter Property="ToolTipService.ShowOnDisabled" Value="True" />

    <Style.Triggers>
      <!-- The header line (spec 4.1) is a title, not a line that refused to run: its own template, so it keeps
           neither the row shape nor the indent of the commands under it, and no hover, pressed or focus visuals.
           Not focusable, so the arrow keys land on the first line that does something. -->
      <DataTrigger Binding="{Binding Kind}" Value="Header">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="MenuItem">
              <StackPanel>
                <StackPanel Margin="10,6,10,6">
                  <!-- One line each, trimmed rather than wrapped: a long file name must not push the commands
                       down the screen. MaxWidth so the menu is no wider than the name it is titled with. -->
                  <TextBlock HorizontalAlignment="Left" MaxWidth="260" FontSize="{StaticResource FontCaption}" FontWeight="SemiBold"
                             Foreground="{StaticResource TextBrush}" Text="{Binding Text}"
                             TextTrimming="CharacterEllipsis" />
                  <TextBlock HorizontalAlignment="Left" MaxWidth="260" Margin="0,1,0,0" FontSize="{StaticResource FontCaption}"
                             Foreground="{StaticResource Text2Brush}" Text="{Binding Detail}"
                             TextTrimming="CharacterEllipsis"
                             Visibility="{Binding Detail, Converter={StaticResource NullToVis}}" />
                </StackPanel>
                <Border Height="1" Margin="6,0,6,4" Background="{StaticResource LineBrush}" />
              </StackPanel>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </DataTrigger>

      <!-- A line that deletes or removes reads in the missing colour, so it is not clicked for another. -->
      <DataTrigger Binding="{Binding IsDestructive}" Value="True">
        <Setter Property="Foreground" Value="{StaticResource MissingFgBrush}" />
      </DataTrigger>

      <DataTrigger Binding="{Binding Kind}" Value="Separator">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="MenuItem">
              <Border Height="1" Margin="6,4" Background="{StaticResource LineBrush}" />
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </DataTrigger>
    </Style.Triggers>
  </Style>

  <!-- One menu for every tile in the app. x:Shared="False" so each StaticResource reference is a fresh instance,
       and no ItemsSource plumbing: a ContextMenu set on an element inherits that element's DataContext, which is
       the tile view model. -->
  <ContextMenu x:Key="TileMenu"
               x:Shared="False"
               ItemContainerStyle="{StaticResource TileMenuItem}"
               ItemsSource="{Binding MenuItems}"
               Style="{StaticResource TileContextMenu}" />

  <!-- A tile in the 360 px panel: two across at 156 with an 8 px gutter is 320, which is the panel's content
       width. The in-game ring and tick are the DataTemplate's, not this style's. -->
  <Style x:Key="PanelTile" TargetType="Border" BasedOn="{StaticResource CardBorder}">
    <Setter Property="Width" Value="156" />
    <Setter Property="Margin" Value="0,0,8,8" />
    <Setter Property="Padding" Value="6" />
  </Style>

  <!-- The two tile actions sit over the picture, so they carry the surface behind them rather than the outline
       button's transparent fill. Smaller than a page button, because at eight columns a tile is narrow. -->
  <Style x:Key="TileAction" TargetType="Button" BasedOn="{StaticResource OutlineButton}">
    <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
    <Setter Property="FontSize" Value="{StaticResource FontCaption}" />
    <Setter Property="Padding" Value="8,3" />
    <Setter Property="Margin" Value="4,0,0,0" />
  </Style>

  <!-- The ground under a picture, wherever one is drawn: Radius 6 everywhere, so a preview is an ImageBrush on a
       Border rather than an Image with square corners, and the tile colour is what an empty slot shows while the
       file is still decoding. Lifted out of MapsView, which had the only copy, because the Packs rows draw
       pictures too. -->
  <Style x:Key="PictureTile" TargetType="Border">
    <Setter Property="Background" Value="{StaticResource TileBrush}" />
    <Setter Property="CornerRadius" Value="{StaticResource Radius}" />
  </Style>

  <!-- Spec 4.1: the one caret, for every fold in the app. One geometry turned rather than two swapped, because
       a turn is what the eye follows; Fold runs it from code, so it is instant when Windows' animations are off. -->
  <Style x:Key="FoldChevron" TargetType="controls:Icon">
    <Setter Property="Margin" Value="0,0,8,0" />
    <Setter Property="VerticalAlignment" Value="Center" />
    <Setter Property="Geometry" Value="{StaticResource Icon.ChevronRight}" />
    <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
    <Setter Property="Size" Value="14" />
    <Setter Property="behaviors:Fold.Chevron" Value="True" />
  </Style>

  <!-- Spec 4: the Backgrounds switch's glyph. A plus whose upright bar scales to nothing as the fold opens, so the
       sign left behind is a minus. Two bars rather than two geometries, because one of them is what animates. -->
  <Style x:Key="PlusMinusGlyph" TargetType="ContentControl">
    <Setter Property="Width" Value="12" />
    <Setter Property="Height" Value="12" />
    <Setter Property="VerticalAlignment" Value="Center" />
    <Setter Property="Focusable" Value="False" />
    <Setter Property="IsHitTestVisible" Value="False" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ContentControl">
          <Grid>
            <Rectangle Height="1.5" VerticalAlignment="Center" Fill="{TemplateBinding Foreground}" />
            <Rectangle Width="1.5"
                       HorizontalAlignment="Center"
                       behaviors:Fold.Upright="True"
                       Fill="{TemplateBinding Foreground}" />
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- A collapsible section's header: a leading chevron, the label and its count, the whole row clickable
       (spec 4). Foreground on the style, so the icon inherits the colour the label states. IsOpen is the fold's
       state for everything inside the template, which is how the caret is told to turn (spec 4.1). -->
  <Style x:Key="SectionHeaderRow" TargetType="ToggleButton">
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="behaviors:Fold.IsOpen" Value="{Binding IsChecked, RelativeSource={RelativeSource Self}}" />
    <Setter Property="FocusVisualStyle" Value="{StaticResource DialogFocusRing}" />
    <Setter Property="Foreground" Value="{StaticResource Text2Brush}" />
    <Setter Property="HorizontalAlignment" Value="Stretch" />
    <Setter Property="HorizontalContentAlignment" Value="Left" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ToggleButton">
          <Border x:Name="Chrome"
                  Background="Transparent"
                  CornerRadius="{StaticResource Radius}"
                  Padding="8,7">
            <StackPanel Orientation="Horizontal">
              <controls:Icon Style="{StaticResource FoldChevron}" />
              <TextBlock FontFamily="{StaticResource Sans}"
                         FontSize="{StaticResource FontBody}"
                         Foreground="{StaticResource Text2Brush}"
                         Text="{Binding Content, RelativeSource={RelativeSource TemplatedParent}}" />
            </StackPanel>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Chrome" Property="Background" Value="{StaticResource Surface2Brush}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <!-- A chip is a ListBoxItem so the list owns which one is on, and ChipToggle above draws it. The toggle is
       chrome only: hit testing and focus belong to the row, which is what Tab and the arrow keys walk. Moved out
       of MapsView.xaml when the two rows pages grew chip rows of their own. -->
  <Style x:Key="ChipRow" TargetType="ListBoxItem">
    <Setter Property="Margin" Value="0,0,8,0" />

    <!-- Owner change O2: the hand the top bar's tabs show. It belongs on the row rather than on ChipToggle,
         which carries one already but is IsHitTestVisible=False, so the cursor under the pointer is the row's. -->
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ListBoxItem">
          <!-- Transparent, not unset: a Grid with no Background is not hit-testable, and both children below are
               IsHitTestVisible=False, so without this the ListBoxItem never sees a mouse click (spec 9). -->
          <Grid Background="Transparent">
            <ToggleButton x:Name="Chip"
                          Content="{TemplateBinding Content}"
                          Focusable="False"
                          IsChecked="{Binding IsSelected, RelativeSource={RelativeSource TemplatedParent}}"
                          IsHitTestVisible="False"
                          Style="{StaticResource ChipToggle}" />
            <Border x:Name="Ring"
                    BorderBrush="{StaticResource FocusRingBrush}"
                    BorderThickness="1"
                    CornerRadius="14"
                    IsHitTestVisible="False"
                    Opacity="0" />
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Chip" Property="Background" Value="{StaticResource Surface2Brush}" />
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <Setter TargetName="Ring" Property="Opacity" Value="0.6" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- Addendum B: Surface, hairline, Radius 6, padding 6 12. Tighter than a card, because a page of them is
       meant to be scanned. -->
  <Style x:Key="RowBorder" TargetType="Border" BasedOn="{StaticResource CardBorder}">
    <Setter Property="Padding" Value="12,6" />
    <Setter Property="Margin" Value="0,0,0,8" />
  </Style>

  <!-- One choice in a row's strip: the picture, the check and the 2 px border when the game is on it, the hover
       actions, and the caption. A Button, so a click applies in one click (addendum B), Enter and Space do the
       same from the keyboard, and the focus ring is the control's own. The size is the page's zoom, read off the
       page rather than set here, so one template serves every step. -->
  <Style x:Key="RowChoiceTileButton" TargetType="Button">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
    <Setter Property="Command" Value="{Binding ApplyCommand}" />
    <Setter Property="ContextMenu" Value="{StaticResource TileMenu}" />
    <Setter Property="ToolTip" Value="{Binding ToolTipText}" />
    <Setter Property="AutomationProperties.Name" Value="{Binding Title}" />
    <Setter Property="VerticalAlignment" Value="Top" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <StackPanel>
            <Grid Width="{Binding DataContext.ThumbWidth, RelativeSource={RelativeSource AncestorType=UserControl}}"
                  Height="{Binding DataContext.ThumbHeight, RelativeSource={RelativeSource AncestorType=UserControl}}">
              <Border Background="{StaticResource TileBrush}" ClipToBounds="True" CornerRadius="{StaticResource Radius}">
                <Image RenderOptions.BitmapScalingMode="HighQuality" Source="{Binding Thumbnail}" Stretch="UniformToFill" />
              </Border>

              <!-- The in-game ring, 2 px in Text, over the picture rather than around the tile, so a strip of
                   tiles stays evenly spaced whichever one the game is on. -->
              <Border x:Name="Ring"
                      BorderBrush="{StaticResource TextBrush}"
                      BorderThickness="2"
                      CornerRadius="{StaticResource Radius}"
                      IsHitTestVisible="False"
                      Opacity="0" />

              <!-- Drawn rather than typed: a glyph would depend on the font having one. -->
              <Border x:Name="Check"
                      Margin="{StaticResource Gap4}"
                      HorizontalAlignment="Right"
                      VerticalAlignment="Top"
                      Background="{StaticResource BgBrush}"
                      CornerRadius="4"
                      Padding="4,3"
                      ToolTip="In game"
                      Visibility="Collapsed">
                <Path Data="M0,3.4 L2.8,6.2 L8,0.6"
                      Stroke="{StaticResource TextBrush}"
                      StrokeEndLineCap="Round"
                      StrokeLineJoin="Round"
                      StrokeStartLineCap="Round"
                      StrokeThickness="1.6" />
              </Border>

              <!-- 3.0: a map whose level places no platforms has nothing a set could change, so the tile keeps
                   its size and says that in words instead of showing an empty picture with an Apply over it. -->
              <TextBlock x:Name="NoArt"
                         Margin="8,0"
                         HorizontalAlignment="Center"
                         VerticalAlignment="Center"
                         FontSize="{StaticResource FontCaption}"
                         Foreground="{StaticResource Text3Brush}"
                         Text="{Binding NoArt}"
                         TextAlignment="Center"
                         TextWrapping="Wrap"
                         Visibility="Collapsed" />

              <!-- Opacity rather than Visibility, the map panel's lesson: a collapsed button cannot be tabbed to,
                   so it would be mouse-only. Revealed by the pointer, by keyboard focus, or by the tile's own
                   menu being open, so the button that opened the menu is still drawn under it. -->
              <StackPanel x:Name="Actions"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          Opacity="0"
                          Orientation="Horizontal">
                <TextBlock x:Name="ApplyLabel"
                           Padding="8,3"
                           VerticalAlignment="Center"
                           Background="{StaticResource SurfaceBrush}"
                           FontSize="{StaticResource FontCaption}"
                           Text="Apply"
                           Visibility="{Binding DataContext.ShowTileApply, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVis}}" />
                <Button AutomationProperties.Name="More actions"
                        controls:TileMenus.OpensMenu="True"
                        Style="{StaticResource TileAction}">
                  <controls:Icon Geometry="{StaticResource Icon.Dots}" Size="14" />
                </Button>
              </StackPanel>
            </Grid>

            <!-- The caption, and after it the word that says the tile's pack is hidden and the tile is here only
                 because the game is showing it (2.8). A Grid rather than one line, so the caption still trims at
                 the tile's width with the word beside it. -->
            <Grid Margin="0,4,0,0"
                  MaxWidth="{Binding DataContext.ThumbWidth, RelativeSource={RelativeSource AncestorType=UserControl}}"
                  HorizontalAlignment="Left">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>

              <TextBlock HorizontalAlignment="Left"
                         FontSize="{StaticResource FontCaption}"
                         Foreground="{StaticResource Text2Brush}"
                         Text="{Binding Title}"
                         TextTrimming="CharacterEllipsis" />

              <TextBlock Grid.Column="1"
                         Margin="4,0,0,0"
                         FontSize="{StaticResource FontCaption}"
                         Foreground="{StaticResource Text2Brush}"
                         Text="{Binding HiddenNote, StringFormat='&#183; {0}'}"
                         Visibility="{Binding IsHiddenPack, Converter={StaticResource BoolToVis}}" />
            </Grid>
          </StackPanel>
          <ControlTemplate.Triggers>
            <DataTrigger Binding="{Binding IsInGame}" Value="True">
              <Setter TargetName="Ring" Property="Opacity" Value="1" />
              <Setter TargetName="Check" Property="Visibility" Value="Visible" />
            </DataTrigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Actions" Property="Opacity" Value="1" />
            </Trigger>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
              <Setter TargetName="Actions" Property="Opacity" Value="1" />
              <Setter TargetName="Ring" Property="Opacity" Value="0.6" />
            </Trigger>
            <DataTrigger Binding="{Binding ContextMenu.IsOpen, RelativeSource={RelativeSource Self}}" Value="True">
              <Setter TargetName="Actions" Property="Opacity" Value="1" />
            </DataTrigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="Actions" Property="Opacity" Value="0" />
            </Trigger>

            <!-- Last, so it wins over the hover and focus triggers above: there is nothing here to act on. The
                 other two tile types carry no such property, hence the fallback. -->
            <DataTrigger Binding="{Binding HasNoArt, FallbackValue=False, TargetNullValue=False}" Value="True">
              <Setter TargetName="NoArt" Property="Visibility" Value="Visible" />
              <Setter TargetName="Actions" Property="Visibility" Value="Collapsed" />
            </DataTrigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Style.Triggers>
      <!-- 3.0: a placeholder tile is not a choice, so it takes no click, opens no menu and names itself to a
           screen reader by the words it draws. -->
      <DataTrigger Binding="{Binding HasNoArt, FallbackValue=False, TargetNullValue=False}" Value="True">
        <Setter Property="Command" Value="{x:Null}" />
        <Setter Property="ContextMenu" Value="{x:Null}" />
        <Setter Property="Cursor" Value="Arrow" />
        <Setter Property="AutomationProperties.Name" Value="No platform art" />
      </DataTrigger>
    </Style.Triggers>
  </Style>

  <!-- "+7" beside a strip with more choices than it had width for. The same height as a thumbnail, so the line of
       them is level. -->
  <Style x:Key="RowMoreTileButton" TargetType="Button" BasedOn="{StaticResource OutlineButton}">
    <Setter Property="FontSize" Value="{StaticResource FontCaption}" />
    <Setter Property="Padding" Value="8,3" />
    <Setter Property="VerticalAlignment" Value="Top" />
    <Setter Property="Height" Value="{Binding DataContext.ThumbHeight, RelativeSource={RelativeSource AncestorType=UserControl}}" />
  </Style>

  <!-- One tile type per template, each deferring to the one above, because an implicit DataTemplate is matched on
       the item's own type. -->
  <DataTemplate DataType="{x:Type vm:MapPictureTileViewModel}">
    <Button Style="{StaticResource RowChoiceTileButton}" />
  </DataTemplate>

  <DataTemplate DataType="{x:Type vm:CustomPictureTileViewModel}">
    <Button Style="{StaticResource RowChoiceTileButton}" />
  </DataTemplate>

  <DataTemplate DataType="{x:Type vm:PlatformSetTileViewModel}">
    <Button Style="{StaticResource RowChoiceTileButton}" />
  </DataTemplate>

  <!-- Addendum B and C: one row, shared by both rows pages. Name column 168 px, then the strip, with the "+N"
       tile floating at the strip's right end rather than in a column of its own: a column would take width from
       the strip, which would change the overflow, which would change the column, which is a layout loop.
       StripPanel keeps MoreWidth clear for it instead. -->
  <DataTemplate x:Key="MapRow">
    <Border controls:RowRealiser.Realise="True" Style="{StaticResource RowBorder}">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="168" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Margin="0,0,12,0" VerticalAlignment="Center">
          <TextBlock FontSize="{StaticResource FontBody}"
                     FontWeight="Medium"
                     Text="{Binding DisplayName}"
                     TextTrimming="CharacterEllipsis"
                     ToolTip="{Binding DisplayName}" />

          <!-- Two tags, because Missing is a style of its own rather than a colour on the ordinary one. Each sits
               in a wrapper that carries the visibility, so neither theme style is derived from. -->
          <Grid MaxWidth="156"
                Margin="0,3,0,0"
                HorizontalAlignment="Left"
                Visibility="{Binding ShowTag, Converter={StaticResource BoolToVis}}">
            <Grid>
              <Grid.Style>
                <Style TargetType="Grid">
                  <Style.Triggers>
                    <DataTrigger Binding="{Binding IsMissing}" Value="True">
                      <Setter Property="Visibility" Value="Collapsed" />
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </Grid.Style>
              <Border Style="{StaticResource StateTagBorder}">
                <TextBlock Text="{Binding TagText}" TextTrimming="CharacterEllipsis" />
              </Border>
            </Grid>
            <Grid>
              <Grid.Style>
                <Style TargetType="Grid">
                  <Setter Property="Visibility" Value="Collapsed" />
                  <Style.Triggers>
                    <DataTrigger Binding="{Binding IsMissing}" Value="True">
                      <Setter Property="Visibility" Value="Visible" />
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </Grid.Style>
              <Border Style="{StaticResource MissingTagBorder}">
                <TextBlock Text="{Binding TagText}" TextTrimming="CharacterEllipsis" />
              </Border>
            </Grid>
          </Grid>
        </StackPanel>

        <Grid Grid.Column="1">
          <ItemsControl Focusable="False" ItemsSource="{Binding Choices}">
            <ItemsControl.ItemsPanel>
              <ItemsPanelTemplate>
                <!-- SetCurrentValue writes Overflow from the measure, so this OneWayToSource binding survives it
                     and the row learns the number without the view reaching into the panel's name scope. -->
                <!-- Spec 4.1: the lines the strip grows fade in and slide up, and only when the row unfolds while
                     it is on screen: a row realised by a scroll has nothing to animate. -->
                <!-- 3.0: a 12 px gutter, the one gap every page's tiles are spaced by. -->
                <controls:StripPanel behaviors:Fold.Animate="True"
                                     behaviors:Fold.IsOpen="{Binding IsUnfolded}"
                                     IsUnfolded="{Binding IsUnfolded}"
                                     Overflow="{Binding Overflow, Mode=OneWayToSource}"
                                     Spacing="12" />
              </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
          </ItemsControl>

          <Button HorizontalAlignment="Right"
                  VerticalAlignment="Top"
                  AutomationProperties.Name="{Binding MoreText}"
                  Command="{Binding UnfoldCommand}"
                  Content="{Binding MoreText}"
                  Style="{StaticResource RowMoreTileButton}"
                  Visibility="{Binding ShowMore, Converter={StaticResource BoolToVis}}" />
        </Grid>
      </Grid>
    </Border>
  </DataTemplate>
</ResourceDictionary>
