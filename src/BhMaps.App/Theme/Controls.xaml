<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:BhMaps.App.Views.Controls"
                    xmlns:converters="clr-namespace:BhMaps.App.Converters"
                    xmlns:sys="clr-namespace:System;assembly=System.Runtime">
  <ResourceDictionary.MergedDictionaries>
    <!-- Fonts as well as Tokens, so this dictionary resolves its own StaticResource references
         whether it is loaded on its own or merged after them by App.xaml. -->
    <ResourceDictionary Source="Fonts.xaml" />
    <ResourceDictionary Source="Tokens.xaml" />
  </ResourceDictionary.MergedDictionaries>

  <!-- Implicit, so plain text anywhere in the app is Geist at the body size. Keyed TextBlock
       styles below derive from it with BasedOn; an explicit Style would otherwise replace it. -->
  <Style TargetType="TextBlock">
    <Setter Property="FontFamily" Value="{StaticResource Sans}" />
    <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
    <Setter Property="FontSize" Value="13" />
  </Style>

  <!-- The button styles below replace the theme's own, so they lose its focus visual with it, and the stock
       dashed adorner is close to invisible on this palette. All three wear this instead, and so do a dialog's
       checkboxes and radios: the same 1 px TextBrush ring at the same radius and the same 60% the tiles and the
       sidebar rows draw, in the adorner layer, so it costs the shared templates nothing (spec 7.8). Declared
       first because a StaticResource cannot reach forward inside one dictionary. -->
  <Style x:Key="DialogFocusRing">
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate>
          <Border Margin="-3"
                  BorderBrush="{StaticResource TextBrush}"
                  BorderThickness="1"
                  CornerRadius="{StaticResource Radius}"
                  Opacity="0.6"
                  SnapsToDevicePixels="True" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- The only filled button. One per page, for that page's single primary action. -->
  <Style x:Key="PrimaryButton" TargetType="Button">
    <Setter Property="FontFamily" Value="{StaticResource Sans}" />
    <Setter Property="FontSize" Value="13" />
    <Setter Property="Background" Value="{StaticResource AccentBrush}" />
    <Setter Property="Foreground" Value="{StaticResource BgBrush}" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Padding" Value="14,7" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="FocusVisualStyle" Value="{StaticResource DialogFocusRing}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <ControlTemplate.Resources>
            <!-- The TextBlock ContentPresenter makes for string content has no logical parent, so the
                 implicit style still reaches it from Application.Resources and beats this button's own
                 Foreground. Templating the string writes those values back as local values, which win. -->
            <!-- Underscores render literally: no keyboard mnemonics in this app, and labels carry pack names. -->
            <DataTemplate DataType="{x:Type sys:String}">
              <TextBlock Text="{Binding}"
                         FontFamily="{Binding FontFamily, RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}}}"
                         FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}}}"
                         Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}}}" />
            </DataTemplate>
          </ControlTemplate.Resources>
          <!-- The canvas draws a 15 px line icon 7 px before a button's label. It is an attached property rather
               than part of the content, so the label stays a string: the accessible name a screen reader reads is
               still the Content, and the icon takes the Foreground the button hands down, hover and disabled with
               it. A button given no glyph collapses the slot and measures exactly as it did before. -->
          <Border x:Name="Chrome"
                  Background="{TemplateBinding Background}"
                  CornerRadius="{StaticResource Radius}"
                  Padding="{TemplateBinding Padding}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Horizontal">
              <controls:Icon x:Name="Glyph"
                             Margin="0,0,7,0"
                             VerticalAlignment="Center"
                             Geometry="{Binding Path=(controls:Icon.Glyph), RelativeSource={RelativeSource TemplatedParent}}"
                             Size="15" />
              <ContentPresenter VerticalAlignment="Center" />
            </StackPanel>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="controls:Icon.Glyph" Value="{x:Null}">
              <Setter TargetName="Glyph" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Chrome" Property="Opacity" Value="0.9" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="Chrome" Property="Opacity" Value="0.4" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="OutlineButton" TargetType="Button">
    <Setter Property="FontFamily" Value="{StaticResource Sans}" />
    <Setter Property="FontSize" Value="13" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderBrush" Value="{StaticResource Line2Brush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
    <Setter Property="Padding" Value="14,7" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="FocusVisualStyle" Value="{StaticResource DialogFocusRing}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <ControlTemplate.Resources>
            <!-- The TextBlock ContentPresenter makes for string content has no logical parent, so the
                 implicit style still reaches it from Application.Resources and beats this button's own
                 Foreground. Templating the string writes those values back as local values, which win. -->
            <DataTemplate DataType="{x:Type sys:String}">
              <TextBlock Text="{Binding}"
                         FontFamily="{Binding FontFamily, RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}}}"
                         FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}}}"
                         Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}}}" />
            </DataTemplate>
          </ControlTemplate.Resources>
          <!-- The icon slot PrimaryButton explains. -->
          <Border x:Name="Chrome"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="{StaticResource Radius}"
                  Padding="{TemplateBinding Padding}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Horizontal">
              <controls:Icon x:Name="Glyph"
                             Margin="0,0,7,0"
                             VerticalAlignment="Center"
                             Geometry="{Binding Path=(controls:Icon.Glyph), RelativeSource={RelativeSource TemplatedParent}}"
                             Size="15" />
              <ContentPresenter VerticalAlignment="Center" />
            </StackPanel>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="controls:Icon.Glyph" Value="{x:Null}">
              <Setter TargetName="Glyph" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Chrome" Property="Background" Value="{StaticResource Surface2Brush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="Chrome" Property="Opacity" Value="0.4" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="PlainButton" TargetType="Button">
    <Setter Property="FontFamily" Value="{StaticResource Sans}" />
    <Setter Property="FontSize" Value="13" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Foreground" Value="{StaticResource Text2Brush}" />
    <Setter Property="Padding" Value="14,7" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="FocusVisualStyle" Value="{StaticResource DialogFocusRing}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <ControlTemplate.Resources>
            <!-- The TextBlock ContentPresenter makes for string content has no logical parent, so the
                 implicit style still reaches it from Application.Resources and beats this button's own
                 Foreground. Templating the string writes those values back as local values, which win. -->
            <DataTemplate DataType="{x:Type sys:String}">
              <TextBlock Text="{Binding}"
                         FontFamily="{Binding FontFamily, RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}}}"
                         FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}}}"
                         Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}}}" />
            </DataTemplate>
          </ControlTemplate.Resources>
          <!-- The icon slot PrimaryButton explains. Hover here lifts the Foreground rather than the fill, and the
               icon follows it, because Stroke is bound to the Foreground this button hands down. -->
          <Border x:Name="Chrome"
                  Background="{TemplateBinding Background}"
                  CornerRadius="{StaticResource Radius}"
                  Padding="{TemplateBinding Padding}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Horizontal">
              <controls:Icon x:Name="Glyph"
                             Margin="0,0,7,0"
                             VerticalAlignment="Center"
                             Geometry="{Binding Path=(controls:Icon.Glyph), RelativeSource={RelativeSource TemplatedParent}}"
                             Size="15" />
              <ContentPresenter VerticalAlignment="Center" />
            </StackPanel>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="controls:Icon.Glyph" Value="{x:Null}">
              <Setter TargetName="Glyph" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="Chrome" Property="Opacity" Value="0.4" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="ChipToggle" TargetType="ToggleButton">
    <Setter Property="FontFamily" Value="{StaticResource Sans}" />
    <Setter Property="FontSize" Value="13" />
    <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
    <Setter Property="BorderBrush" Value="{StaticResource LineBrush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Foreground" Value="{StaticResource Text2Brush}" />
    <Setter Property="Padding" Value="12,5" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ToggleButton">
          <ControlTemplate.Resources>
            <!-- See PrimaryButton: the checked chip's Bg-coloured label needs the same handover. -->
            <DataTemplate DataType="{x:Type sys:String}">
              <TextBlock Text="{Binding}"
                         FontFamily="{Binding FontFamily, RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}}}"
                         FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}}}"
                         Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}}}" />
            </DataTemplate>
          </ControlTemplate.Resources>
          <Border x:Name="Chrome"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="14"
                  Padding="{TemplateBinding Padding}">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Chrome" Property="Background" Value="{StaticResource Surface2Brush}" />
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="Chrome" Property="Background" Value="{StaticResource TextBrush}" />
              <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource TextBrush}" />
              <Setter Property="Foreground" Value="{StaticResource BgBrush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="Chrome" Property="Opacity" Value="0.4" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- The tick box on a map card (spec 3.1): an 18 px box over the preview, filled in Text with a Bg tick when
       the map is ticked. No focus visual of its own, because the box is Focusable="False" and the card behind it
       is the tab stop. -->
  <Style x:Key="CardTick" TargetType="CheckBox">
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="CheckBox">
          <Border x:Name="Box"
                  Width="18"
                  Height="18"
                  Background="{StaticResource BgBrush}"
                  BorderBrush="{StaticResource Line2Brush}"
                  BorderThickness="1"
                  CornerRadius="4"
                  Opacity="0.95">
            <Path x:Name="Tick"
                  Width="10"
                  Height="7"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"
                  Data="M0,3.4 L2.8,6.2 L8,0.6"
                  Stretch="Uniform"
                  Stroke="{StaticResource BgBrush}"
                  StrokeEndLineCap="Round"
                  StrokeLineJoin="Round"
                  StrokeStartLineCap="Round"
                  StrokeThickness="1.6"
                  Visibility="Collapsed" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="Box" Property="Background" Value="{StaticResource TextBrush}" />
              <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource TextBrush}" />
              <Setter TargetName="Tick" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource TextBrush}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <converters:IsCurrentPageConverter x:Key="IsCurrentPage" />

  <!-- One of the top bar's four destinations (spec 2.1). A Button, not a ToggleButton: the open page is a
       DataTrigger on the shell's CurrentPage, and a ToggleButton would clobber a one-way IsChecked binding on
       its first click. Tag carries the page this tab opens. -->
  <Style x:Key="TopBarTab" TargetType="Button">
    <Setter Property="FontFamily" Value="{StaticResource Sans}" />
    <Setter Property="FontSize" Value="13" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="Foreground" Value="{StaticResource Text2Brush}" />
    <Setter Property="Padding" Value="12,5" />
    <Setter Property="Margin" Value="0,0,4,0" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <ControlTemplate.Resources>
            <!-- See PrimaryButton: the implicit TextBlock style would beat the button's own Foreground. -->
            <DataTemplate DataType="{x:Type sys:String}">
              <TextBlock Text="{Binding}"
                         FontFamily="{Binding FontFamily, RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}}}"
                         FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}}}"
                         Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}}}" />
            </DataTemplate>
          </ControlTemplate.Resources>
          <Grid>
            <Border x:Name="Chrome"
                    Background="{TemplateBinding Background}"
                    CornerRadius="{StaticResource Radius}"
                    Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <Border x:Name="Ring"
                    BorderBrush="{StaticResource TextBrush}"
                    BorderThickness="1"
                    CornerRadius="{StaticResource Radius}"
                    IsHitTestVisible="False"
                    Opacity="0" />
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <Setter TargetName="Ring" Property="Opacity" Value="0.6" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <!-- Hover first, the open page second, so the open tab keeps its fill while the pointer is over it. A
         trigger's binding cannot reach its TemplatedParent, which is why both live out here. -->
    <Style.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter Property="Background" Value="{StaticResource Surface2Brush}" />
        <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
      </Trigger>
      <DataTrigger Value="True">
        <DataTrigger.Binding>
          <MultiBinding Converter="{StaticResource IsCurrentPage}">
            <Binding Path="Tag" RelativeSource="{RelativeSource Self}" />
            <Binding Path="CurrentPage" />
          </MultiBinding>
        </DataTrigger.Binding>
        <Setter Property="Background" Value="{StaticResource TileBrush}" />
        <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
      </DataTrigger>
    </Style.Triggers>
  </Style>

  <Style x:Key="CardBorder" TargetType="Border">
    <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
    <Setter Property="BorderBrush" Value="{StaticResource LineBrush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="CornerRadius" Value="{StaticResource Radius}" />
  </Style>

  <!-- The bar a page floats over its grid while maps are ticked (spec 3.3). Visibility belongs to the page. -->
  <Style x:Key="SelectionBar" TargetType="Border" BasedOn="{StaticResource CardBorder}">
    <Setter Property="Padding" Value="12,8" />
  </Style>

  <!-- Style.Resources reaches the tag's own TextBlock child, so a tag is one Border plus a TextBlock. -->
  <Style x:Key="StateTagBorder" TargetType="Border">
    <Style.Resources>
      <Style TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource Sans}" />
        <Setter Property="FontSize" Value="11" />
        <Setter Property="Foreground" Value="{StaticResource Text2Brush}" />
      </Style>
    </Style.Resources>
    <Setter Property="Background" Value="{StaticResource Surface2Brush}" />
    <Setter Property="CornerRadius" Value="4" />
    <Setter Property="Padding" Value="6,2" />
  </Style>

  <!-- Missing is the only coloured state (D8). -->
  <Style x:Key="MissingTagBorder" TargetType="Border">
    <Style.Resources>
      <Style TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource Sans}" />
        <Setter Property="FontSize" Value="11" />
        <Setter Property="Foreground" Value="{StaticResource MissingFgBrush}" />
      </Style>
    </Style.Resources>
    <Setter Property="Background" Value="{StaticResource MissingBgBrush}" />
    <Setter Property="CornerRadius" Value="4" />
    <Setter Property="Padding" Value="6,2" />
  </Style>

  <Style x:Key="FieldTextBox" TargetType="TextBox">
    <Setter Property="FontFamily" Value="{StaticResource Sans}" />
    <Setter Property="FontSize" Value="13" />
    <Setter Property="Background" Value="{StaticResource Surface2Brush}" />
    <Setter Property="BorderBrush" Value="{StaticResource Line2Brush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
    <Setter Property="CaretBrush" Value="{StaticResource TextBrush}" />
    <Setter Property="SelectionBrush" Value="{StaticResource Text3Brush}" />
    <Setter Property="Padding" Value="10,6" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="TextBox">
          <Border x:Name="Chrome"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="{StaticResource Radius}">
            <!-- No Margin binding here on purpose: TextBoxBase puts the TextBox's Padding onto PART_ContentHost
                 itself when it attaches the render scope, so binding Padding to Margin as well applied the inset
                 twice and started typed text 33 px past this field's left padding (spec 9). The stock templates
                 leave this alone for the same reason. -->
            <ScrollViewer x:Name="PART_ContentHost"
                          Focusable="False"
                          HorizontalScrollBarVisibility="Hidden"
                          VerticalScrollBarVisibility="Hidden" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
              <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource TextBrush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="Chrome" Property="Opacity" Value="0.4" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- Selection is a ring, not a fill, so a tile's own artwork keeps its colours. Keyboard focus
       draws the same ring at 60% so focus is visible even on an unselected tile (spec 7.8). -->
  <Style x:Key="TileListBoxItem" TargetType="ListBoxItem">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="Padding" Value="0" />
    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
    <Setter Property="VerticalContentAlignment" Value="Stretch" />
    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ListBoxItem">
          <Grid Background="{TemplateBinding Background}">
            <ContentPresenter Margin="{TemplateBinding Padding}"
                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
            <Border x:Name="Ring"
                    BorderBrush="{StaticResource TextBrush}"
                    BorderThickness="1"
                    CornerRadius="{StaticResource Radius}"
                    IsHitTestVisible="False"
                    Opacity="0" />
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <Setter TargetName="Ring" Property="Opacity" Value="0.6" />
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
              <Setter TargetName="Ring" Property="Opacity" Value="1" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="TileListBox" TargetType="ListBox">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Padding" Value="0" />
    <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
    <Setter Property="ItemContainerStyle" Value="{StaticResource TileListBoxItem}" />
    <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
  </Style>

  <Style x:Key="ZoomSliderStyle" TargetType="Slider">
    <Setter Property="Width" Value="96" />
    <Setter Property="Orientation" Value="Horizontal" />
    <Setter Property="IsSnapToTickEnabled" Value="True" />
    <Setter Property="TickFrequency" Value="1" />
    <Setter Property="VerticalAlignment" Value="Center" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Slider">
          <ControlTemplate.Resources>
            <!-- The two halves of the groove are invisible; the track line is drawn once, behind. -->
            <Style x:Key="GrooveHalf" TargetType="RepeatButton">
              <Setter Property="Focusable" Value="False" />
              <Setter Property="IsTabStop" Value="False" />
              <Setter Property="Template">
                <Setter.Value>
                  <ControlTemplate TargetType="RepeatButton">
                    <Border Background="Transparent" />
                  </ControlTemplate>
                </Setter.Value>
              </Setter>
            </Style>
          </ControlTemplate.Resources>
          <Grid x:Name="Chrome" Height="20" Background="Transparent">
            <Border Height="2"
                    VerticalAlignment="Center"
                    Background="{StaticResource Line2Brush}"
                    CornerRadius="1" />
            <Track x:Name="PART_Track">
              <Track.DecreaseRepeatButton>
                <RepeatButton Command="Slider.DecreaseLarge" Style="{StaticResource GrooveHalf}" />
              </Track.DecreaseRepeatButton>
              <Track.IncreaseRepeatButton>
                <RepeatButton Command="Slider.IncreaseLarge" Style="{StaticResource GrooveHalf}" />
              </Track.IncreaseRepeatButton>
              <Track.Thumb>
                <Thumb Width="12" Height="12" Focusable="False">
                  <Thumb.Template>
                    <ControlTemplate TargetType="Thumb">
                      <Ellipse Fill="{StaticResource TextBrush}" />
                    </ControlTemplate>
                  </Thumb.Template>
                </Thumb>
              </Track.Thumb>
            </Track>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="Chrome" Property="Opacity" Value="0.4" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="SectionHeaderText" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
    <Setter Property="FontSize" Value="12" />
    <Setter Property="FontWeight" Value="Medium" />
    <Setter Property="Foreground" Value="{StaticResource Text3Brush}" />
  </Style>

  <Style x:Key="MonoText" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
    <Setter Property="FontFamily" Value="{StaticResource Mono}" />
    <Setter Property="FontSize" Value="12" />
    <Setter Property="Foreground" Value="{StaticResource Text2Brush}" />
  </Style>

  <!-- Spec section 13: the menus are themed rather than left as the platform's. BasedOn the Fluent implicit style,
       the lesson Views/AddPicturesWindow.xaml line 40 records: without it these fall off the Fluent theme
       (ThemeMode=Dark in App.xaml) onto the legacy template, which on this palette draws a light menu. Setters
       only, no re-template: the roles a MenuItem template carries (submenu, check column, icon column) are not
       worth re-implementing for a five line menu. -->
  <Style x:Key="TileContextMenu" TargetType="ContextMenu" BasedOn="{StaticResource {x:Type ContextMenu}}">
    <Setter Property="FontFamily" Value="{StaticResource Sans}" />
    <Setter Property="FontSize" Value="13" />
    <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
    <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
    <Setter Property="BorderBrush" Value="{StaticResource Line2Brush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Padding" Value="4" />
  </Style>

  <!-- Header and Command are setters rather than per item markup, so one ItemsSource of TileMenuCommand drives
       every tile's menu and a page never writes a MenuItem by hand. A literal MenuItem that sets its own Header
       still wins: a local value beats a style setter. -->
  <Style x:Key="TileMenuItem" TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}">
    <Setter Property="FontFamily" Value="{StaticResource Sans}" />
    <Setter Property="FontSize" Value="13" />
    <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
    <Setter Property="Padding" Value="10,6" />
    <Setter Property="Header" Value="{Binding Text}" />
    <Setter Property="Command" Value="{Binding Command}" />
    <Setter Property="AutomationProperties.Name" Value="{Binding Text}" />
  </Style>

  <!-- One menu for every tile in the app. x:Shared="False" so each StaticResource reference is a fresh instance,
       and no ItemsSource plumbing: a ContextMenu set on an element inherits that element's DataContext, which is
       the tile view model. -->
  <ContextMenu x:Key="TileMenu"
               x:Shared="False"
               ItemContainerStyle="{StaticResource TileMenuItem}"
               ItemsSource="{Binding MenuItems}"
               Style="{StaticResource TileContextMenu}" />

  <!-- One segment of a two or three way control (spec 3.2 and 5). The host is a Surface2 Border of Padding 2 with
       these side by side inside it; the chosen one is the Surface pill. Part C's pack detail uses the same style
       for Combined / Backgrounds / Platforms, so nothing about it names a page. -->
  <Style x:Key="SegmentControl" TargetType="ToggleButton">
    <Setter Property="FontFamily" Value="{StaticResource Sans}" />
    <Setter Property="FontSize" Value="12" />
    <Setter Property="Foreground" Value="{StaticResource Text2Brush}" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="Padding" Value="14,5" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="FocusVisualStyle" Value="{StaticResource DialogFocusRing}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ToggleButton">
          <ControlTemplate.Resources>
            <!-- See PrimaryButton: a string ContentPresenter needs the Foreground handed to it as a local value. -->
            <DataTemplate DataType="{x:Type sys:String}">
              <TextBlock Text="{Binding}"
                         FontFamily="{Binding FontFamily, RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}}}"
                         FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}}}"
                         Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}}}" />
            </DataTemplate>
          </ControlTemplate.Resources>
          <Border x:Name="Chrome"
                  Background="{TemplateBinding Background}"
                  CornerRadius="4"
                  Padding="{TemplateBinding Padding}">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="Chrome" Property="Background" Value="{StaticResource SurfaceBrush}" />
              <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="Chrome" Property="Opacity" Value="0.4" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- A tile in the 360 px panel: two across at 156 with an 8 px gutter is 320, which is the panel's content
       width. The in-game ring and tick are the DataTemplate's, not this style's. -->
  <Style x:Key="PanelTile" TargetType="Border" BasedOn="{StaticResource CardBorder}">
    <Setter Property="Width" Value="156" />
    <Setter Property="Margin" Value="0,0,8,8" />
    <Setter Property="Padding" Value="6" />
  </Style>

  <!-- A collapsible section's header: a leading chevron, the label and its count, the whole row clickable
       (spec 4). Two chevron geometries rather than one rotated, so there is no transform origin to get wrong.
       Foreground on the style, so the icon inherits the colour the label states. -->
  <Style x:Key="SectionHeaderRow" TargetType="ToggleButton">
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
    <Setter Property="Foreground" Value="{StaticResource Text2Brush}" />
    <Setter Property="HorizontalAlignment" Value="Stretch" />
    <Setter Property="HorizontalContentAlignment" Value="Left" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ToggleButton">
          <Grid>
            <Border x:Name="Chrome"
                    Background="Transparent"
                    CornerRadius="{StaticResource Radius}"
                    Padding="8,7">
              <StackPanel Orientation="Horizontal">
                <controls:Icon x:Name="Chevron"
                               Margin="0,0,8,0"
                               VerticalAlignment="Center"
                               Geometry="{StaticResource Icon.ChevronRight}"
                               Size="14" />
                <TextBlock FontFamily="{StaticResource Sans}"
                           FontSize="13"
                           Foreground="{StaticResource Text2Brush}"
                           Text="{Binding Content, RelativeSource={RelativeSource TemplatedParent}}" />
              </StackPanel>
            </Border>
            <Border x:Name="Ring"
                    BorderBrush="{StaticResource TextBrush}"
                    BorderThickness="1"
                    CornerRadius="{StaticResource Radius}"
                    IsHitTestVisible="False"
                    Opacity="0" />
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="Chevron" Property="Geometry" Value="{StaticResource Icon.ChevronDown}" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Chrome" Property="Background" Value="{StaticResource Surface2Brush}" />
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <Setter TargetName="Ring" Property="Opacity" Value="0.6" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
